<IfModule mod_rewrite.c>
  RewriteEngine On

  # Serve existing files & folders normally
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Redirect all other requests to React entry point
  RewriteRule ^ index.html [L]
</IfModule>
